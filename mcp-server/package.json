{
  "name": "nala-diff-judge-mcp",
  "version": "1.0.0",
  "description": "MCP server that fetches nala-auto screenshot-diff pairs so any MCP-capable AI client (Copilot CLI, Claude Desktop, etc.) can visually judge whether a diff is a real regression or noise.",
  "type": "module",
  "bin": {
    "nala-diff-judge-mcp": "./index.js"
  },
  "main": "index.js",
  "scripts": {
    "start": "node index.js"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.30.1"
  },
  "engines": {
    "node": ">=18"
  }
}
